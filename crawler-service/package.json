{
  "name": "testgen-crawler",
  "version": "1.0.0",
  "description": "Playwright-based crawler service for TestGen Studio",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browsers": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "@playwright/test": "^1.44.0",
    "express": "^4.18.2",
    "playwright": "^1.44.0"
  },
  "engines": {
    "node": ">=18"
  }
}
